
<%= simple_form_for([@trip, @task],
  data: {
    tasks_target: "form",
    action: "submit->tasks#send"
  }) do |f| %>
    <%= f.input :description,
                placeholder: "Add new task" %>
    <%= f.submit "Add" %>
<% end %>
